/**
 * 체험용 제조처 스크리닝 — **실제 화면 그대로**, 조회도 실제로 돈다.
 *
 * 여기만 살려 두는 이유: 조회가 이 도구의 핵심 체험이고, 트렌드 화면과 달리
 *   - 유튜브 쿼터를 쓰지 않는다(식품안전나라 공공 API),
 *   - 컨택 후보는 localStorage 에만 쌓여 공용 데이터를 건드리지 않는다.
 *
 * 실제 화면 컴포넌트를 그대로 재사용한다 — 복제하면 둘이 갈린다.
 */
export { default } from "@/app/odm/page";

export const metadata = { title: "크림보드 체험 · 제조처 스크리닝" };
